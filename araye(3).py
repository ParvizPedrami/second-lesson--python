import random
Array = []
n= int(input('please enter the number:'))
while true:
    number= random.randint(0,n)
    if num not in Array:
        Array.append(number)

        if len(array)==n:
            print(array)
            break
